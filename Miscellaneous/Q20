Q20-Write a Python program to calculate arc length of an angle.

from math import*
def arclength():
    pie=22/7
    diameter = float(input('Diameter of circle: '))
    angle = float(input('angle measure: '))
    if angle >= 360:
        print("Angle is not possible")
        return
    arc_length = (pie*diameter) * (angle/360)
    print("Arc Length is: ", arc_length)

arclength()
